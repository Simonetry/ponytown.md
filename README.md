/* S!DETRACKED · PONYTOWN_PROTOCOL v?.??
 <br>status: passive_sync
   <br>@target: pony.town/rules */

// /* INTERACTIONS */
   <br>`[boop/sit/lay]` : FF (feel free) to
   <br>`[hide/cuddle]`  : ask first
   <br>`[roleplay]`     : disabled (does not rp)
   <br>`[shipping]`     : plush/proximity =//= ship

// /* AGE_RESTRICTIONS */
   <br>`[if_18+]`       : platonic flirting = allowed (inc. kisses)
                    <br> *note: respect "no" immediately
 <br>  `[if_<18]`       : flirting/nsfw = restricted
            <br>         *action: instant block

// /* COMMUNICATION_LOGS */
  <br> `[convo]`        : interact first = required (may yap)
 <br>  `[friending]`    : if 18+ && repeat_chat = FF
 <br>  `[discord]`      : if high interaction = FF to ask

// /* WARNING */
 <br>  > `system_access`
<br>   > do not request specific alters

<br>✰ Active skin represents alter that's around - so long it's stated "🐇 sys" or "🐇" after
<br>✰ Often AFK/off-tab, whisper when set on [away] to int
